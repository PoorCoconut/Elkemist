name: Export and Deploy to Itch.io

on:
  push:
    branches:
      - main

env:
  GODOT_VERSION: 4.6.2
  EXPORT_NAME: index.html

jobs:
  export-web:
    name: Web Export
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:4.6.2
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Export Templates
        run: |
          mkdir -v -p ~/.local/share/godot/export_templates
          mv /root/.local/share/godot/export_templates/${GODOT_VERSION}.stable ~/.local/share/godot/export_templates/${GODOT_VERSION}.stable

      - name: Web Build
        run: |
          mkdir -v -p build/web
          godot --headless --export-release "Web" build/web/$EXPORT_NAME

      - name: Deploy to Itch.io
        env:
          # This grabs your secure key from GitHub Secrets
          BUTLER_API_KEY: ${{ secrets.ITCHIO_API_KEY }}
        # This natively pushes the 'build/web' folder to your exact URL routing
        run: butler push build/web poorcoco/elkemy:html5